# homebrew-tap

Homebrew tap for [Solo-it](https://github.com/Solo-it) tools.

```bash
brew install Solo-it/tap/backupctl
```

Formulas in `Formula/` are published automatically by
[GoReleaser](https://goreleaser.com/) from each tool's release workflow —
don't edit them by hand.
